version: "3.9"

services:
  web:
    build: .
    container_name: todo-app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - todo-data:/app/data
    restart: unless-stopped

volumes:
  todo-data:
